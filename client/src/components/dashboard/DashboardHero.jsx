const DashboardHero = () => (
  <section className="dashboard-hero">
    <div>
      <p className="landing-eyebrow">Operations Dashboard</p>
      <h1 className="dashboard-title">Command center redesigned for faster coordination.</h1>
      <p className="dashboard-subtitle">
        Priorities, team movement, and closures are separated into clean modules so coordinators can scan and act instantly.
      </p>
    </div>
  </section>
);

export default DashboardHero;

